package com.fsd.stock.exchange;

import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

//import com.fsd.stock.exchange.model.Stockexchange;
//import com.fsd.stock.exchange.repository.ExchangeRepository;

@RunWith(SpringRunner.class)
@SpringBootTest
public class ExchangeApplicationTests {
//	@Autowired
//	private ExchangeRepository userRepository;

	@Test
	public void contextLoads() {
//		userRepository.save(new Stockexchange("BSE", "SEstockExchangeBrief", "SEcontactAddress", "SEremarks"));
//		userRepository.save(new Stockexchange("NSE", "SEstockExchangeBrief", "SEcontactAddress", "SEremarks"));
//		Assert.assertEquals(3, userRepository.findAll().size());
	}

}
